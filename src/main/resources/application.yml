spring:
    liquibase:
        change-log: classpath:liquibase/changelog.yml
    datasource:
        driver-class-name: org.postgresql.Driver
        url: jdbc:postgresql://localhost:5432/btc
        username: postgres
        password: test